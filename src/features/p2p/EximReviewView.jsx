import React, { useEffect, useMemo, useState } from 'react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { Anchor, ArrowLeft, CheckCircle2, Download, Eye, FileText, Loader2, Search, Upload, UserPlus, X } from 'lucide-react';
import { apiFetch } from '../../services/api';
import { useToast } from '../../components/ui/toast';

const statusLabel = (value) => ({ submitted: 'Submitted', exim_review: 'EXIM Review', assigned_to_agent: 'With Customs Agent', material_received: 'Material Received', custom_cleared: 'Customs Cleared' }[value] || String(value || '').replaceAll('_', ' '));
const statusClass = (value) => value === 'custom_cleared' ? 'bg-emerald-50 text-emerald-700' : value === 'assigned_to_agent' ? 'bg-cyan-50 text-cyan-700' : 'bg-teal-50 text-[#0d7676]';
const dateText = (value, withTime = false) => value ? new Date(value).toLocaleString('en-IN', withTime ? { dateStyle: 'medium', timeStyle: 'short' } : { dateStyle: 'medium' }) : '—';

function AssignModal({ entry, agents, onClose, onSaved }) {
  const { showToast } = useToast();
  const [agentId, setAgentId] = useState(entry.customAgentId || '');
  const [notes, setNotes] = useState(entry.eximNotes || '');
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');
  const assign = async () => {
    if (!agentId) return setError('Select an active customs agent.');
    setSaving(true); setError('');
    try {
      const response = await apiFetch(`/api/p2p/exim/bl-entries/${entry.blId}/assign`, { method: 'POST', body: JSON.stringify({ agentId, notes }) });
      const json = await response.json();
      if (!response.ok) throw new Error(json.error || 'Agent assignment failed.');
      showToast({ type: 'success', title: 'Agent Assigned', description: `${entry.blNumber} is now available in the customs agent portal.` });
      onSaved(json.data);
    } catch (e) { setError(e.message); } finally { setSaving(false); }
  };
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/50 p-4"><div className="w-full max-w-md overflow-hidden rounded-2xl bg-white shadow-xl"><div className="flex items-start justify-between border-b p-5"><div><h2 className="text-base font-extrabold">Assign to Customs Agent</h2><p className="mt-1 text-xs text-slate-500">Select the agent handling customs clearance for BL {entry.blNumber}.</p></div><button onClick={onClose} className="rounded-lg p-1 text-slate-400 hover:bg-slate-100"><X className="h-5 w-5" /></button></div><div className="space-y-4 p-5">{error && <div className="rounded-lg bg-rose-50 p-3 text-xs font-semibold text-rose-700">{error}</div>}<label className="block text-xs font-bold">Select Agent *<select value={agentId} onChange={(e) => setAgentId(e.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2.5 outline-none focus:border-teal-400"><option value="">Search and select agent...</option>{agents.map((agent) => <option key={agent.agentId} value={agent.agentId}>{agent.agencyName} — {agent.contactPerson || agent.email}</option>)}</select></label><label className="block text-xs font-bold">Notes for Agent<textarea value={notes} onChange={(e) => setNotes(e.target.value)} rows="3" placeholder="Instructions for customs clearance..." className="mt-1.5 w-full rounded-lg border border-slate-200 bg-slate-50 p-3 outline-none focus:border-teal-400" /></label></div><div className="flex justify-end gap-2 border-t p-4"><button onClick={onClose} className="rounded-lg border px-4 py-2 text-xs font-bold">Cancel</button><button onClick={assign} disabled={saving || !agentId} className="inline-flex items-center gap-1 rounded-lg bg-[#0d7676] px-4 py-2 text-xs font-bold text-white disabled:opacity-50">{saving ? <Loader2 className="h-4 w-4 animate-spin" /> : <UserPlus className="h-4 w-4" />}Assign Agent</button></div></div></div>;
}

function EximList() {
  const navigate = useNavigate();
  const [entries, setEntries] = useState([]); const [agents, setAgents] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [query, setQuery] = useState(''); const [status, setStatus] = useState(''); const [selected, setSelected] = useState(null);
  const load = async () => { setLoading(true); try { const response = await apiFetch('/api/p2p/exim/bl-entries'); const json = await response.json(); if (!response.ok) throw new Error(json.error || 'Unable to load BL entries.'); setEntries(json.data || []); setAgents(json.agents || []); } catch (e) { setError(e.message); } finally { setLoading(false); } };
  useEffect(() => { load(); }, []);
  const filtered = useMemo(() => entries.filter((entry) => (!status || entry.status === status) && [entry.blNumber, entry.rfqNumber, entry.rfqId, entry.vendorName].some((value) => String(value || '').toLowerCase().includes(query.toLowerCase()))), [entries, query, status]);
  return <div className="space-y-5 pb-12"><div><h1 className="flex items-center gap-2 text-xl font-extrabold"><Anchor className="h-5 w-5 text-[#0d7676]" />EXIM — BL Entry Review</h1><p className="text-xs text-slate-500">Review submitted BL entries from vendors and assign them for customs clearance.</p></div>{error && <div className="rounded-xl border border-rose-200 bg-rose-50 p-3 text-xs font-semibold text-rose-700">{error}</div>}<section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-wrap gap-2 border-b p-4"><label className="flex w-72 items-center gap-2 rounded-lg border bg-slate-50 px-3"><Search className="h-4 w-4 text-slate-400" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search BL #, vendor, RFQ..." className="w-full bg-transparent py-2.5 text-xs outline-none" /></label><select value={status} onChange={(e) => setStatus(e.target.value)} className="w-44 rounded-lg border bg-slate-50 px-3 text-xs"><option value="">All Status</option><option value="submitted">Submitted</option><option value="exim_review">EXIM Review</option><option value="assigned_to_agent">With Customs Agent</option><option value="custom_cleared">Customs Cleared</option></select></div>{loading ? <div className="p-14 text-center"><Loader2 className="mx-auto h-5 w-5 animate-spin text-[#0d7676]" /></div> : <div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="bg-slate-50 text-[10px] uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">#</th><th>BL Number</th><th>RFQ</th><th>Vendor</th><th className="text-center">Containers</th><th>Status</th><th>Assigned Agent</th><th>Date</th><th className="px-4 text-center">Actions</th></tr></thead><tbody>{filtered.map((entry, index) => <tr key={entry.blId} className="border-t hover:bg-slate-50/70"><td className="px-4 py-4 text-slate-400">{index + 1}</td><td className="font-bold">{entry.blNumber}</td><td><span className="rounded border bg-slate-50 px-2 py-1 font-mono text-[10px]">{entry.rfqNumber || entry.rfqId}</span></td><td>{entry.vendorName || '—'}</td><td className="text-center"><span className="inline-flex h-7 min-w-7 items-center justify-center rounded-full bg-cyan-50 font-bold text-[#0d7676]">{entry.containerCount}</span></td><td><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${statusClass(entry.status)}`}>{statusLabel(entry.status)}</span></td><td>{entry.customAgentName ? <span className="rounded border border-teal-200 bg-teal-50 px-2 py-1 text-[10px] font-semibold text-[#0d7676]">{entry.customAgentName}</span> : <span className="text-slate-400">Unassigned</span>}</td><td className="text-slate-500">{dateText(entry.createdAt)}</td><td className="px-4 text-center"><button onClick={() => navigate(`/admin/exim/${entry.blId}`)} className="rounded-lg border p-2 text-slate-500 hover:border-teal-300 hover:text-[#0d7676]" title="View BL"><Eye className="h-4 w-4" /></button></td></tr>)}</tbody></table>{!filtered.length && <div className="p-12 text-center text-xs text-slate-400">No BL entries found.</div>}</div>}</section>{selected && <AssignModal entry={selected} agents={agents} onClose={() => setSelected(null)} onSaved={() => { setSelected(null); load(); }} />}</div>;
}

function EximDetail({ blId }) {
  const { showToast } = useToast();
  const [entry, setEntry] = useState(null); const [agents, setAgents] = useState([]); const [error, setError] = useState(''); const [loading, setLoading] = useState(true); const [assigning, setAssigning] = useState(false); const [docType, setDocType] = useState(''); const [file, setFile] = useState(null); const [uploading, setUploading] = useState(false);
  const load = async () => { setLoading(true); try { const response = await apiFetch(`/api/p2p/exim/bl-entries/${blId}`); const json = await response.json(); if (!response.ok) throw new Error(json.error || 'BL entry not found.'); setEntry(json.data); setAgents(json.agents || []); } catch (e) { setError(e.message); } finally { setLoading(false); } };
  useEffect(() => { load(); }, [blId]);
  const upload = async () => { if (!docType || !file) return setError('Select a document type and file.'); setUploading(true); setError(''); try { const response = await apiFetch(`/api/p2p/exim/bl-entries/${blId}/documents`, { method: 'POST', body: JSON.stringify({ documents: [{ docType, fileName: file.name }] }) }); const json = await response.json(); if (!response.ok) throw new Error(json.error); showToast({ type: 'success', title: 'Documents Uploaded', description: 'The EXIM review record was updated.' }); setDocType(''); setFile(null); await load(); } catch (e) { setError(e.message); } finally { setUploading(false); } };
  if (loading) return <div className="p-12 text-center"><Loader2 className="mx-auto h-5 w-5 animate-spin text-[#0d7676]" /></div>;
  if (!entry) return <div className="rounded-xl bg-rose-50 p-4 text-xs font-bold text-rose-700">{error}</div>;
  return <div className="space-y-5 pb-12"><div className="flex flex-wrap items-end justify-between gap-3"><div><Link to="/admin/exim" className="inline-flex items-center gap-1 text-xs text-slate-500 hover:text-[#0d7676]"><ArrowLeft className="h-4 w-4" />EXIM Review</Link><div className="mt-2 flex items-center gap-2"><h1 className="text-xl font-extrabold">BL: {entry.blNumber}</h1><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${statusClass(entry.status)}`}>{statusLabel(entry.status)}</span></div><p className="text-xs text-slate-500">{entry.rfqNumber || entry.rfqId} · {entry.vendorName || '—'} · {entry.containerCount} containers</p></div><div className="flex gap-2"><Link to={`/admin/rfqs/${entry.rfq?.rfqNumber || entry.rfqId}`} className="rounded-lg px-3 py-2 text-xs font-bold text-[#0d5bd7]">View RFQ →</Link>{entry.status !== 'custom_cleared' && <button onClick={() => setAssigning(true)} className="inline-flex items-center gap-1 rounded-lg bg-[#0d7676] px-4 py-2 text-xs font-bold text-white"><UserPlus className="h-4 w-4" />{entry.customAgentId ? 'Reassign Agent' : 'Assign to Agent'}</button>}</div></div>{error && <div className="rounded-xl border border-rose-200 bg-rose-50 p-3 text-xs font-semibold text-rose-700">{error}</div>}<section className="grid gap-4 rounded-2xl border bg-white p-5 text-xs shadow-sm sm:grid-cols-3"><div><p className="text-slate-400">EXIM Reviewed</p><strong>{dateText(entry.eximReviewedAt, true)}</strong></div><div><p className="text-slate-400">Assigned At</p><strong>{dateText(entry.assignedAt, true)}</strong></div><div><p className="text-slate-400">Customs Cleared At</p><strong className={entry.customsClearedAt ? 'text-emerald-700' : 'text-slate-400'}>{dateText(entry.customsClearedAt, true)}</strong></div>{entry.customAgentName && <div><p className="text-slate-400">Assigned Agent</p><strong>{entry.customAgentName}</strong></div>}{entry.eximNotes && <div className="sm:col-span-2"><p className="text-slate-400">EXIM Notes</p><strong>{entry.eximNotes}</strong></div>}</section><section className="overflow-hidden rounded-2xl border bg-white shadow-sm"><h2 className="border-b px-5 py-4 text-sm font-extrabold">All Documents ({entry.documents?.length || 0})</h2><div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="bg-slate-50 text-[10px] uppercase text-slate-500"><tr><th className="px-4 py-3">Type</th><th>Stage</th><th>Uploaded By</th><th>File</th><th>Date</th><th className="px-4">Action</th></tr></thead><tbody>{entry.documents?.map((doc, index) => <tr key={`${doc.fileUrl}-${index}`} className="border-t"><td className="px-4 py-4 font-semibold">{doc.docType}</td><td>{doc.stage || (String(doc.uploadedBy).includes('Customs Agent') ? 'Customs Agent' : 'Vendor Submission')}</td><td>{doc.uploadedBy || 'Vendor'}</td><td className="max-w-xs truncate font-mono text-[10px]">{doc.fileUrl}</td><td>{dateText(doc.uploadedAt, true)}</td><td className="px-4"><button className="inline-flex items-center gap-1 rounded border px-2 py-1 text-[#0d5bd7]"><Download className="h-3 w-3" />Download</button></td></tr>)}</tbody></table></div></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="flex items-center gap-2 text-sm font-extrabold"><Upload className="h-4 w-4" />Upload EXIM Documents</h2><div className="mt-4 grid gap-3 sm:grid-cols-[1fr_1fr_auto]"><select value={docType} onChange={(e) => setDocType(e.target.value)} className="rounded-lg border bg-slate-50 px-3 py-2.5 text-xs"><option value="">Document type</option><option>Commercial Invoice</option><option>Packing List</option><option>Certificate of Origin</option><option>Insurance Certificate</option><option>Other EXIM Document</option></select><input type="file" onChange={(e) => setFile(e.target.files?.[0] || null)} className="rounded-lg border bg-slate-50 px-3 py-2 text-xs" /><button onClick={upload} disabled={uploading || !docType || !file} className="rounded-lg bg-[#0d7676] px-4 py-2 text-xs font-bold text-white disabled:opacity-50">{uploading ? 'Uploading...' : 'Upload Documents'}</button></div></section>{assigning && <AssignModal entry={entry} agents={agents} onClose={() => setAssigning(false)} onSaved={(updated) => { setAssigning(false); setEntry((current) => ({ ...current, ...updated })); }} />}</div>;
}

export default function EximReviewView() {
  const { blId } = useParams();
  return blId ? <EximDetail blId={blId} /> : <EximList />;
}
